import datetime

day = int(input("Nhập ngày: "))
month = int(input("Nhập tháng: "))
year = int(input("Nhập năm: "))

# Dùng datetime để xử lý
ngay = datetime.date(year, month, day)
ngay_ke_tiep = ngay + datetime.timedelta(days=1)

print("Ngày kế sau là:", ngay_ke_tiep.strftime("%d/%m/%Y"))

